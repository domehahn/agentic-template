# OpenHands Agent Instructions

Project: {{ project_name }}

OpenHands must make actual file changes when implementation is requested. Do not provide only a tutorial.

## Rules

- Use the workspace tools to inspect, edit, and validate files.
- Do not commit, push, or create pull requests unless explicitly requested.
- Keep the diff small and scoped.
- Do not access secrets, `.env`, private keys, credentials, tokens, dumps, or production logs.
- Run relevant validation commands when practical.

## Local model note

If using a local model such as Ollama, prefer explicit prompts that require tool use and visible diffs.
